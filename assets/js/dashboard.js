(function(){const types=new Set(['image/jpeg','image/png','image/webp','image/avif']);const limit=5*1024*1024;function validateImage(file){if(!file)return'Bir görsel seçin.';if(!types.has(file.type))return'Yalnızca JPG, PNG, WebP veya AVIF yükleyebilirsiniz.';if(file.size>limit)return'Görsel en fazla 5 MiB olabilir.';return null}function showImage(container,source,alt){const image=document.createElement('img');image.src=source;image.alt=alt;container.replaceChildren(image)}window.NoshutdownDashboard={validateImage,showImage}})();
