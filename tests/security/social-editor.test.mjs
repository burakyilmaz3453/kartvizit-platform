import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';const read=p=>readFile(new URL(`../../${p}`,import.meta.url),'utf8');
test('social editor converts existing columns and preserves order',async()=>{const js=await read('assets/js/social-editor.js');assert.match(js,/function fromProfile/);assert.match(js,/social_order/);assert.match(js,/function serialize/);for(const p of ['linkedin','instagram','twitter','facebook'])assert.match(js,new RegExp(p))});
test('social editor uses safe DOM APIs and accessible movement controls',async()=>{const js=await read('assets/js/social-editor.js');assert.doesNotMatch(js,/innerHTML\s*=/);assert.match(js,/createElement/);assert.match(js,/yukarı taşı/);assert.match(js,/aşağı taşı/);assert.match(js,/aria-label/)});
